import { describe, expect, it } from "vitest";

describe("admin-api file stub", () => {
  it("is intentionally empty because coverage moved to module-specific tests", () => {
    expect(true).toBe(true);
  });
});
